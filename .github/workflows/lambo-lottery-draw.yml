name: Lambo Lottery Daily Draw

on:
  schedule:
    # JAVÍTVA: Minden nap 21:10-kor fut le budapesti idő szerint.
    # Figyelem: Ez 22:52 UTC időpontot jelent a nyári időszámítás (CEST) alatt.
    # Téli időszámítás (CET) alatt ez 20:10 UTC lesz.
    - cron: '39 19 * * *'
  workflow_dispatch: # Engedélyezi a manuális indítást is.

jobs:
  lottery-draw:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Setup pnpm
      uses: pnpm/action-setup@v2
      with:
        version: 8
        
    - name: Install dependencies
      run: pnpm install
      
    # A KÜLÖN IDŐELLENŐRZŐ LÉPÉS EL LETT TÁVOLÍTVA.
    # A workflow most már csak a megadott időpontban fog elindulni, 
    # így a belső ellenőrzés felesleges és hibalehetőséget rejt magában.
      
    - name: Run lottery draw
      # Az "if" feltételt eltávolítottuk, mert ha a job elindul, akkor futnia kell.
      env:
        DATABASE_URL: ${{ secrets.DATABASE_URL }}
        FORCE_DRAW_NOW: 'true'
      run: node scripts/lambo-lottery-draw.js --force-now
      
    - name: Notify on success
      # Csak akkor jelez sikert, ha a teljes job sikeres volt.
      if: success()
      run: echo "🎰 Lottery draw completed successfully at 21:10!"
      
    - name: Notify on failure
      # Csak akkor jelez hibát, ha a job hibára futott.
      if: failure()
      run: echo "❌ Lottery draw failed!"